export type JsonplaceholderCommentResponseModel = JsonplaceholderCommentModel[];

export interface JsonplaceholderCommentModel {
	name: string;
	postId: number;
	id: number;
	body: string;
	email: string;
}

